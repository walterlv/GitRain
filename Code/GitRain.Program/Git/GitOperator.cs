using System.Windows;

namespace Cvte.GitRain.Git
{
    public static class GitOperator
    {
        /// <summary>
        /// 同步一整个 Git 仓库，这包括所有的分支和子模块。
        /// </summary>
        public static void SyncWholeRepo()
        {
            MessageBox.Show("假装正在同步……");
        }
    }
}
